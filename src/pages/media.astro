---
import BaseLayout from "../layouts/BaseLayout.astro";
import YouTube from "../components/YouTube.astro";

// 👉 Remplace par tes VRAIS IDs YouTube
const videos = [
  { id: "YE6cIhBlYJU", title: "Tainted Nevro Love" },
  { id: "JsVPydqa_LQ", title: "Class vioc 2024" },
];
---
<BaseLayout title="Médias — Nevrosyne">
  <h1 class="text-3xl font-bold mb-6">Médias</h1>

  <section class="space-y-10">
    <div>
      <h2 class="text-xl font-semibold mb-3">Clips</h2>
      <div class="grid md:grid-cols-2 gap-6">
        {videos.map(v => <YouTube id={v.id} title={v.title} />)}
      </div>
    </div>
  </section>
</BaseLayout>